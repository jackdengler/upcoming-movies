{
  "name": "upcoming-movies",
  "private": true,
  "version": "0.0.0",
  "description": "Test tooling for the upcoming-movies PWA. The app itself stays vanilla HTML/CSS/JS — these dev dependencies only run Playwright.",
  "scripts": {
    "serve": "npx --yes http-server -p 4173 -c-1 --silent .",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0",
    "http-server": "^14.1.1"
  }
}
